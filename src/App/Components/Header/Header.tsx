import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <header>
            <h1 className="text-center">Cotiza monedas al instante</h1>
        </header>
    );
};

export default Header;
